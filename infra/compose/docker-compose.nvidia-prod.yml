services:
  transcode_runner:
    image: ${REGISTRY:-tztcloud}/transcode-runner-nvidia:${TAG:-v1.3.1}
    container_name: ${TRANSCODE_CONTAINER_NAME:-transcode-runner}
    restart: unless-stopped
    runtime: nvidia
    environment:
      RUNNER_ADDR: :8080
      MAX_QUEUE_SIZE: ${TRANSCODE_MAX_QUEUE_SIZE:-2}
      TEMP_DIR: /tmp/transcode
      JOB_TTL_SECONDS: ${TRANSCODE_JOB_TTL_SECONDS:-3600}
      PRESETS_FILE: /etc/runner/presets/${TRANSCODE_PRESETS_FILE:-nvidia-gtx1080-transcode.yaml}
      NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
      NVIDIA_DRIVER_CAPABILITIES: ${NVIDIA_DRIVER_CAPABILITIES:-compute,video,utility}
    volumes:
      - ../presets:/etc/runner/presets:ro
      - transcode-temp:/tmp/transcode
    ports:
      - "${TRANSCODE_RUNNER_PORT:-8086}:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    logging:
      driver: json-file
      options:
        max-size: ${LOG_MAX_SIZE:-10m}
        max-file: ${LOG_MAX_FILE:-5}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: ${NVIDIA_DEVICE_COUNT:-1}
              capabilities: [gpu]

  abr_runner:
    image: ${REGISTRY:-tztcloud}/abr-runner-nvidia:${TAG:-v1.3.1}
    container_name: ${ABR_CONTAINER_NAME:-abr-runner}
    restart: unless-stopped
    runtime: nvidia
    environment:
      RUNNER_ADDR: :8080
      MAX_QUEUE_SIZE: ${ABR_MAX_QUEUE_SIZE:-1}
      TEMP_DIR: /tmp/abr
      JOB_TTL_SECONDS: ${ABR_JOB_TTL_SECONDS:-3600}
      PRESETS_FILE: /etc/runner/presets/${ABR_PRESETS_FILE:-nvidia-gtx1080-abr.yaml}
      NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
      NVIDIA_DRIVER_CAPABILITIES: ${NVIDIA_DRIVER_CAPABILITIES:-compute,video,utility}
    volumes:
      - ../presets:/etc/runner/presets:ro
      - abr-temp:/tmp/abr
    ports:
      - "${ABR_RUNNER_PORT:-8087}:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    logging:
      driver: json-file
      options:
        max-size: ${LOG_MAX_SIZE:-10m}
        max-file: ${LOG_MAX_FILE:-5}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: ${NVIDIA_DEVICE_COUNT:-1}
              capabilities: [gpu]

  live_runner:
    image: ${REGISTRY:-tztcloud}/live-runner-nvidia:${TAG:-v1.3.1}
    container_name: ${LIVE_CONTAINER_NAME:-live-runner}
    restart: unless-stopped
    runtime: nvidia
    environment:
      RUNNER_ADDR: :8080
      PUBLIC_HOST: ${LIVE_PUBLIC_HOST:-localhost:${LIVE_RUNNER_PORT:-8088}}
      TEMP_DIR: /tmp/live
      PRESETS_FILE: /etc/runner/presets/${LIVE_PRESETS_FILE:-live.yaml}
      RTMP_PUBLIC_HOST: ${LIVE_RTMP_PUBLIC_HOST:-localhost}
      RTMP_LISTEN_HOST: 0.0.0.0
      RTMP_PORT_START: ${LIVE_RTMP_PORT_START:-19350}
      RTMP_PORT_END: ${LIVE_RTMP_PORT_END:-19449}
      NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
      NVIDIA_DRIVER_CAPABILITIES: ${NVIDIA_DRIVER_CAPABILITIES:-compute,video,utility}
    volumes:
      - ../presets:/etc/runner/presets:ro
      - live-temp:/tmp/live
    ports:
      - "${LIVE_RUNNER_PORT:-8088}:8080"
      - "${LIVE_RTMP_PORT_START:-19350}-${LIVE_RTMP_PORT_END:-19449}:${LIVE_RTMP_PORT_START:-19350}-${LIVE_RTMP_PORT_END:-19449}"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    logging:
      driver: json-file
      options:
        max-size: ${LOG_MAX_SIZE:-10m}
        max-file: ${LOG_MAX_FILE:-5}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: ${NVIDIA_DEVICE_COUNT:-1}
              capabilities: [gpu]

volumes:
  transcode-temp: {}
  abr-temp: {}
  live-temp: {}
